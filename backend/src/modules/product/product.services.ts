const productCreateIntoDB = (reqBody: any, userId: number) => {
  console.log('req body :', reqBody, 'userId :', userId);
  try {
  } catch (error) {}
};

export const productService = { productCreateIntoDB };
